extends ../main/layout
include ../mixins/videos

block content
  div#videoContainer
    video(src="/" + video.fileUrl)
    div#videoControls 
      button#play Play 
      button#mute Mute 
      input(type="range",step="0.1", min="0", max="1", value=0.5)#volume
      div
        span#currentTime 00:00 
        span | 
        span#totalTime 00:00
      div
        input(type="range",step="1", min="0", value=0)#timeLine
      div   
        button#fullScreen Full Screen
  //- div
  //-   p=video.description
  //-   small=video.createdAt 
  //- div
  //-   small Uploaded by 
  //-     a(href=`/users/${video.owner.id}`)=video.owner.name
  //- //- 상대경로 절대경로의 차이 (/root를 붙여주느냐 안붙여주느냐의 차이)
  //- if String(video.owner._id) === String(loggedInUser._id)
  //-   a(href=`${video.id}/edit`) 비디오 수정 &larr;
  //-   br
  //-   a(href=`${video.id}/delete`) 비디오 삭제 &larr;

block scripts 
  script(src="/static/js/videoPlayer.js")